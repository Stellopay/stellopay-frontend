import en from "./en.json";

/**
 * Primary messages dictionary module.
 * Holds English string copy for UI components before full `next-intl` runtime integration.
 */
export const messages = en;
export type Messages = typeof en;

export default messages;
