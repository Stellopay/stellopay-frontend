# TODO

- [ ] Add fallback render for unsupported tokens in `components/transactions/token-icon.tsx`.
- [ ] Ensure component always returns JSX (no undefined return).
- [ ] Add simple test or update existing tests if coverage exists for `TokenIcon` (optional depending on repo patterns).
- [ ] Run relevant unit tests and/or lint if configured.

